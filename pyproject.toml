[project]
name = "mastering-transformers"
version = "0.1.0"
description = "Development repository for Mastering Transformers book"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "jupyterlab>=4.5.7",
    "torch>=2.12.0",
    "transformers>=5.9.0",
]
